
## Install Homebrew
```shell
zsh: command not found: brew
```
Solution: 安装 Homebrew：https://brew.sh/


## Install `tree`
```
zsh: command not found: tree
```
Solution: `brew install tree`


## Install `dropbox`
```shell
brew install --cask dropbox
```

## Install Foxit 
```shell
brew install --cask foxit-pdf-editor
```
installer: Error - Foxit PDF Editor can’t be installed on this computer.


## Install Maven
```shell
brew install maven
```